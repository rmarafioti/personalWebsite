/*import { Outlet } from "react-router-dom";
import Navbar from "./Navbar";
import Footer from "./Footer";

/**
 * @function Root
 * @description the Root function returns the stucture of the site setting the navbar as a universal component in each view and the Outlet holding the paths for each child component as a different view
 *
 */

/*export default function Root() {
  return (
    <>
      <Navbar />
      <main>
        <Outlet />
      </main>
      <Footer />
    </>
  );
}*/
